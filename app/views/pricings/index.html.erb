     
      <section class="common-banner-sec text-center" style="background-image:url(/assets/pricing-information-banner.png);">
         <div class="banner-text-parent">
            <div class="banner-text-child">
               <h1>Ship Anything, Anytime, Anywhere</h1>
               <p>There is no hidden place where you wouldn’t go, and where your parcel will not reach.  </p>
            </div>
         </div>
      </section>
      <!-- Main Start -->
      <main>
         <section class="treveller-section common-section">
            <div class="container">
               <div class="static-page-head text-center">
                  <h2 class="common-content-heading">BringItFast Price Chart</h2>
               </div>
			         <div class="clearfix">
        <fieldset class="global-fieldset">
        <legend>Filter</legend>
        <div class="filter-form">
        <!-- <form action="/admin/users" method="get" accept-charset="UTF-8" enctype="multipart/form-data" id="web-user-val" class="form-common clearfix"> -->
         <%= form_for "/pricings", method: :get,  class:"form-common clearfix", remote: true do |f| %>
          <!-- <div class="col-sm-3">
            <div class="form-group">
              <label>NAME</label>
              <input type="text" name="search" class="form-control custom-control" placeholder="SEARCH COUNTRY" id="country_serch">
            </div>
          </div> -->


            <div class="filter-block">
          <div class="form-group select-setting">
              <div class="col-sm-4">
                <label>Select Country</label>
                
                 <%= f.select(:country_id , @country_names, {:include_blank => false, :prompt => "Select country"}, { :class => 'select-dropdown form-control' }) %>
              </div>
          </div>
        </div>
        <div class="filter-block">
          <div class="form-group select-setting">
              <div class="col-sm-4">
                <label>Select Weight Limit</label>
              
                 <%= f.select(:pricing_information_id , "", {:include_blank => false , :prompt => "Select weight-limit" }, { :class => 'select-dropdown form-control', :id => "weight_dropdown"}) %>
              </div>
          </div>
        </div>
         
          <div class="col-sm-3">
            <div class="filter-btn">
              <button class="btn btn-default"> <i class="fa fa-search"></i> Filter </button>
            </div>
          </div>
          
         
        <!-- </form> -->
        <%end%>
      </div>
    </fieldset>
      </div>
              <div id="pricing-content">
                 <%= render partial: "index"%>
               </div>
			  
            </div>
         </section>
         <!-- Account Section End -->
         
      </main>
      <!-- Main End -->
      

<script>
 $("#_pricings_country_id").change(function(){
    var country_name = $(this).val();
     $.ajax({
      url:  "/pricings/find_weight_from_country",
      type: "GET",
      data: { country_name: country_name

      },
      success: function(response){
        var options="";
      $.each(response.data, function(key, value){

    options+= '<option value="'+key+'">'+value+'</option>'

});
      // alert(options);
      $("#weight_dropdown").empty();
      $("#weight_dropdown").html(options);


      }

       
     });
 });

</script>